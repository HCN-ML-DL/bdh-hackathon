warning: in the working copy of 'train.py', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/train.py b/train.py[m
[1mindex 2a79a2d..d17fdda 100644[m
[1m--- a/train.py[m
[1m+++ b/train.py[m
[36m@@ -1,5 +1,5 @@[m
 """[m
[31m-BDH Training Script - Memory Optimized for L4[m
[32m+[m[32mBDH Training Script - FAST Mode for L4 (23GB VRAM)[m
 """[m
 [m
 import torch[m
[36m@@ -8,20 +8,28 @@[m [mfrom datasets import load_from_disk[m
 from bdh_model import BDH[m
 from tqdm import tqdm[m
 import os[m
[32m+[m[32mimport glob[m
[32m+[m
[32m+[m[32m# SPEED OPTIMIZATIONS[m
[32m+[m[32mtorch.backends.cudnn.benchmark = True[m
[32m+[m[32mtorch.backends.cudnn.deterministic = False[m
[32m+[m[32mtorch.backends.cuda.matmul.allow_tf32 = True[m
[32m+[m[32mtorch.backends.cudnn.allow_tf32 = True[m
[32m+[m[32mtorch.set_float32_matmul_precision('medium')[m
 [m
 # ============================================================[m
[31m-# CONFIG - Reduced for L4 (23GB VRAM)[m
[32m+[m[32m# CONFIG - Optimized for L4 speed[m
 # ============================================================[m
 CONFIG = {[m
[31m-    "n_neurons": 16384,      # Reduced from 32k to 16k[m
[31m-    "d_internal": 192,       # Reduced from 256 to 192[m
[31m-    "num_layers": 4,         # Reduced from 6 to 4[m
[31m-    "num_heads": 4,          # Reduced from 8 to 4[m
[31m-    "batch_size": 4,         # Reduced from 16 to 4[m
[31m-    "seq_len": 256,          # Reduced from 512 to 256[m
[31m-    "lr": 1e-3,[m
[32m+[m[32m    "n_neurons": 16384,[m
[32m+[m[32m    "d_internal": 192,[m
[32m+[m[32m    "num_layers": 4,[m
[32m+[m[32m    "num_heads": 4,[m
[32m+[m[32m    "batch_size": 32,[m
[32m+[m[32m    "seq_len": 256,[m
[32m+[m[32m    "lr": 3e-4,          # Lower LR for continued training[m
     "epochs": 3,[m
[31m-    "grad_accum": 8,         # Increased to compensate for smaller batch[m
[32m+[m[32m    "grad_accum": 2,[m
     "save_every": 500,[m
 }[m
 [m
[36m@@ -47,18 +55,49 @@[m [mclass TextDataset(Dataset):[m
         return x, y[m
 [m
 [m
[32m+[m[32mdef find_latest_checkpoint():[m
[32m+[m[32m    """Find checkpoint with highest step number."""[m
[32m+[m[32m    checkpoints = glob.glob("checkpoints/bdh_step_*.pt") + glob.glob("checkpoints/bdh_final.pt")[m
[32m+[m[41m    [m
[32m+[m[32m    if not checkpoints:[m
[32m+[m[32m        return None, 0[m
[32m+[m[41m    [m
[32m+[m[32m    def get_step(path):[m
[32m+[m[32m        if "final" in path:[m
[32m+[m[32m            try:[m
[32m+[m[32m                ckpt = torch.load(path, map_location="cpu")[m
[32m+[m[32m                return ckpt.get("step", 0)[m
[32m+[m[32m            except:[m
[32m+[m[32m                return 0[m
[32m+[m[32m        try:[m
[32m+[m[32m            return int(path.split("_")[-1].replace(".pt", ""))[m
[32m+[m[32m        except:[m
[32m+[m[32m            return 0[m
[32m+[m[41m    [m
[32m+[m[32m    latest = max(checkpoints, key=get_step)[m
[32m+[m[32m    return latest, get_step(latest)[m
[32m+[m
[32m+[m
 def main():[m
     device = "cuda"[m
     torch.cuda.empty_cache()[m
     [m
     print(f"GPU: {torch.cuda.get_device_name(0)}")[m
     print(f"VRAM: {torch.cuda.get_device_properties(0).total_memory / 1e9:.1f} GB")[m
[32m+[m[32m    print("TF32: ON | cuDNN benchmark: ON | Deterministic: OFF")[m
     [m
     # Load data[m
     print("\nLoading dataset...")[m
     dataset = load_from_disk("./tinystories_data")[m
     train_data = TextDataset(dataset, CONFIG["seq_len"])[m
[31m-    train_loader = DataLoader(train_data, batch_size=CONFIG["batch_size"], shuffle=True, num_workers=2, pin_memory=True)[m
[32m+[m[32m    train_loader = DataLoader([m
[32m+[m[32m        train_data,[m[41m [m
[32m+[m[32m        batch_size=CONFIG["batch_size"],[m[41m [m
[32m+[m[32m        shuffle=True,[m[41m [m
[32m+[m[32m        num_workers=4,[m
[32m+[m[32m        pin_memory=True,[m
[32m+[m[32m        persistent_workers=True[m
[32m+[m[32m    )[m
     [m
     # Create model[m
     print("\nInitializing model...")[m
[36m@@ -68,17 +107,31 @@[m [mdef main():[m
         d_internal=CONFIG["d_internal"],[m
         num_layers=CONFIG["num_layers"],[m
         num_heads=CONFIG["num_heads"],[m
[31m-        dropout=0.0,  # No dropout for faster training[m
[32m+[m[32m        dropout=0.0,[m
     ).to(device)[m
     [m
[31m-    optimizer = torch.optim.AdamW(model.parameters(), lr=CONFIG["lr"], weight_decay=0.1)[m
[31m-    [m
     os.makedirs("checkpoints", exist_ok=True)[m
     global_step = 0[m
     [m
[32m+[m[32m    # Resume from LATEST checkpoint[m
[32m+[m[32m    resume_path, resume_step = find_latest_checkpoint()[m
[32m+[m[32m    if resume_path:[m
[32m+[m[32m        print(f"\nResuming from {resume_path} (step {resume_step})...")[m
[32m+[m[32m        ckpt = torch.load(resume_path, map_location=device)[m
[32m+[m[32m        model.load_state_dict(ckpt["model"])[m
[32m+[m[32m        global_step = resume_step[m
[32m+[m[41m    [m
[32m+[m[32m    # Use lower LR if resuming[m
[32m+[m[32m    lr = CONFIG["lr"] if global_step == 0 else CONFIG["lr"] / 3[m
[32m+[m[32m    print(f"Using LR: {lr}")[m
[32m+[m[41m    [m
[32m+[m[32m    optimizer = torch.optim.AdamW(model.parameters(), lr=lr, weight_decay=0.1, fused=True)[m
[32m+[m[32m    scaler = torch.amp.GradScaler('cuda')[m
[32m+[m[41m    [m
     print("\n" + "="*60)[m
     print("TRAINING STARTED")[m
     print(f"Effective batch size: {CONFIG['batch_size'] * CONFIG['grad_accum']}")[m
[32m+[m[32m    print(f"Starting from step: {global_step}")[m
     print("="*60)[m
     [m
     for epoch in range(CONFIG["epochs"]):[m
[36m@@ -86,17 +139,26 @@[m [mdef main():[m
         pbar = tqdm(train_loader, desc=f"Epoch {epoch+1}")[m
         [m
         for batch_idx, (x, y) in enumerate(pbar):[m
[31m-            x, y = x.to(device), y.to(device)[m
[32m+[m[32m            x, y = x.to(device, non_blocking=True), y.to(device, non_blocking=True)[m
[32m+[m[41m            [m
[32m+[m[32m            with torch.amp.autocast('cuda'):[m
[32m+[m[32m                logits, loss, _ = model(x, y)[m
[32m+[m[32m                loss = loss / CONFIG["grad_accum"][m
[32m+[m[41m            [m
[32m+[m[32m            # NaN check[m
[32m+[m[32m            if torch.isnan(loss):[m
[32m+[m[32m                print("\n⚠️ NaN detected! Skipping batch...")[m
[32m+[m[32m                optimizer.zero_grad(set_to_none=True)[m
[32m+[m[32m                continue[m
             [m
[31m-            # forward() now returns (logits, loss, states)[m
[31m-            logits, loss, _ = model(x, y)[m
[31m-            loss = loss / CONFIG["grad_accum"][m
[31m-            loss.backward()[m
[32m+[m[32m            scaler.scale(loss).backward()[m
             [m
             if (batch_idx + 1) % CONFIG["grad_accum"] == 0:[m
[31m-                torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)[m
[31m-                optimizer.step()[m
[31m-                optimizer.zero_grad()[m
[32m+[m[32m                scaler.unscale_(optimizer)[m
[32m+[m[32m                torch.nn.utils.clip_grad_norm_(model.parameters(), 0.5)  # Tighter clipping[m
[32m+[m[32m                scaler.step(optimizer)[m
[32m+[m[32m                scaler.update()[m
[32m+[m[32m                optimizer.zero_grad(set_to_none=True)[m
                 global_step += 1[m
             [m
             pbar.set_postfix({"loss": f"{loss.item() * CONFIG['grad_accum']:.3f}", "step": global_step})[m
